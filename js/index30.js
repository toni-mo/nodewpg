// Defining variables
let squareSide, squareArea, templateMessage;

squareSide = 5;

squareArea = Math.pow(squareSide, 2);

templateMessage = ` squareSide = ${squareSide}cm; \n squareArea = ${squareArea}cm`;

// Displaying variables
console.log(templateMessage);